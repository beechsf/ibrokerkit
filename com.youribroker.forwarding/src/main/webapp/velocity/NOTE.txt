These files are Velocity templates used by the Forwarding i-service to render its pages.
For each file, a short description is provided, as well as a list of variables that are
set when the template is loaded:

TEMPLATE FILES
==============

forwarding-base.vm:
-------------------

This is the "base" template used by all other templates. It should contain elements that you
want to appear on all "sub-" templates, e.g. your logo, basic design, links to your terms of
services, etc.

What it must contain at a minimum:
- A <div wicket:id="velocity"></div> element. This is where the "sub-" templates will be
inserted.

What variables are set:
- All variables from your application.properties file.
- The $encoder utility object.

forwarding-head.vm:
-------------------

This template is used to render the <head> section of all other templates. It can contain
<title>, <link>, etc.

What it must contain at a minimum:
(nothing)

What variables are set:
- All variables from your application.properties file.
- The $encoder utility object.

forwarding-index.vm:
--------------------

This template is used when a user accesses your i-service without any path 
(e.g. http://forwarding.youribroker.com/). You should use this to explain the service and 
possibly link to further information.

What it must contain at a minimum:
(nothing)

What variables are set:
- All variables from your application.properties file.
- The $encoder utility object.

forwarding-error.vm:
--------------------

This template is used when a problem occurs in the i-service web application. There are
different kinds of errors. You should use this to explain to the user what has happened.

What it must contain at a minimum:
(nothing)

What variables are set:
- All variables from your application.properties file.
- The $encoder utility object.
- One of the following: $exexpired, $exinternal, $exexception
-- $exexpired means that the session with the user has expired
-- $exinternal means that an internal error in the web framework has occurred
-- $exexception means that an exception was raised while running the web application. In
   this case, additional variables are set:
   -- $exclass the Java class of the exception
   -- $extime the timestamp of the exception
   -- $expath the URI path at which the exception was raised
   -- $exmessage the human readable message associated with the exception
   -- $extrace the detailed stack trace of the exception

forwarding-forwardingindex.vm:
------------------------------

This template is used to render the Forwarding Index Page for an i-name at path /(+index), if
the Index Page flag is enabled. You should use this to list all path -> uri mappings of the
Forwarding i-service.

What it must contain at a minimum:
(nothing)

What variables are set:
- All variables from your application.properties file.
- The $encoder utility object.
- $qxri: The i-name for which the Forwarding i-service is being requested.
- $forwarding: The Forwarding object from iserviceStore. It has the following fields:
-- $forwarding.id: The internal ID of the i-service.
-- $forwarding.timestamp: The timestamp of the i-service.
-- $forwarding.qxri: The QXRI field of the i-service.
-- $forwarding.indx: The INDX field of the i-service.
-- $forwarding.name: The internal name of the i-service.
-- $forwarding.enabled: The enabled flag of the i-service.
-- $forwarding.attributes: The attributes of the i-service.
-- $forwarding.indexPage: The indexPage flag of the Forwarding i-service.
-- $forwarding.errorPage: The errorPage flag of the Forwarding i-service.
-- $forwarding.mappings: The path -> uri mappings of the Forwarding i-service.

forwarding-forwardingerror.vm:
------------------------------

This template is used to render the Forwarding Error Page for an i-name at a path for which
no mapping is defined, if the Error Page flag is enabled.

What it must contain at a minimum:
(nothing)

What variables are set:
- All variables from your application.properties file.
- The $encoder utility object.
- $qxri: The i-name for which the Forwarding i-service is being requested.
- $path: The path that was requested but for which no mapping is defined.
- $forwarding: The Forwarding object from iserviceStore. It has the following fields:
-- $forwarding.id: The internal ID of the i-service.
-- $forwarding.timestamp: The timestamp of the i-service.
-- $forwarding.qxri: The QXRI field of the i-service.
-- $forwarding.indx: The INDX field of the i-service.
-- $forwarding.name: The internal name of the i-service.
-- $forwarding.enabled: The enabled flag of the i-service.
-- $forwarding.attributes: The attributes of the i-service.
-- $forwarding.indexPage: The indexPage flag of the Forwarding i-service.
-- $forwarding.errorPage: The errorPage flag of the Forwarding i-service.
-- $forwarding.mappings: The path -> uri mappings of the Forwarding i-service.

forwarding-notfound.vm:
---------------------------

This template is used when
- A Forwarding i-service is requested for an i-name that does not exist.
- A Forwarding i-service is requested for an i-name that exists but has no Forwarding
  i-service.
- The enabled flag is not set for the requested Forwarding i-service.
 
What it must contain at a minimum:
(nothing)

What variables are set:
- All variables from your application.properties file.
- The $encoder utility object.
- $qxri: The i-name for which the Forwarding i-service is being requested.

<custom>.vm:
------------

You can add arbitrary additional templates and link to them. This can be used for static
pages such as a Terms of Service page, etc. For example, if you create a file named "tos.vm",
it will be accessible under /page/name/tos

What it must contain at a minimum:
(nothing)

What variables are set:
- All variables from your application.properties file.
- The $encoder utility object.

UTILITY OBJECTS
===============

The following utility objects can be used by your templates:

$encoder
--------

- $encoder.html(string): HTML-encodes a string so that it can be displayed on a web page.
